# Copy this file to registry-deploy.env (gitignored, not committed) and fill in real values.
# Read by pull-latest-registry.sh to deploy docker-compose.registry.yaml without committing
# environment-specific URLs to git.

SIGNALR_CLIENT_ENDPOINT=https://signalr-hub.example.net
IMAGE_PREFIX=registry.example.net/registry/signalr-fun
